let role="",tab="bookings",timerInterval=null;
const $=s=>document.querySelector(s),esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
async function api(u,o={}){const h={...(o.headers||{})};if(!(o.body instanceof FormData)&&!h["content-type"])h["content-type"]="application/json";const r=await fetch(u,{...o,credentials:"same-origin",headers:h}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||(r.status===404?`API маршрут не знайдено: ${u}`:r.status===403?"Недостатньо прав для цієї операції":`HTTP ${r.status}`));return d}
function msg(t,e=false){$("#msg").innerHTML=`<div class="message ${e?"error":"success"}">${esc(t)}</div>`}
function postBooking(id,action,extra={}){return api("/api/admin/bookings",{method:"POST",body:JSON.stringify({id,action,...extra})})}
async function login(){try{const d=await api("/api/auth/login",{method:"POST",body:JSON.stringify({username:$("#loginUser").value.trim(),password:$("#loginPass").value})});role=d.role;$("#role").textContent=role;const rb=$("#resetTabBtn");if(rb)rb.style.display=role==="superadmin"?"inline-flex":"none";$("#login").classList.add("hidden");$("#panel").classList.remove("hidden");load()}catch(e){$("#loginMsg").textContent="❌ "+e.message}}
async function load(){try{clearInterval(timerInterval);const u={analytics:"/api/admin/analytics",market:"/api/admin/market",bookings:"/api/admin/bookings",history:"/api/admin/history",completed:"/api/admin/completed-works",reviews:"/api/admin/reviews",works:"/api/admin/works",services:"/api/admin/services",mechanics:"/api/admin/mechanics",logs:"/api/admin/logs",blocks:"/api/admin/blocks",reset:"/api/admin/reset",ai:null},f={analytics:renderAnalytics,market:renderMarket,bookings:renderBookings,history:renderHistory,completed:renderCompletedWorks,reviews:renderReviews,works:renderWorks,services:renderServices,mechanics:renderMechanics,logs:renderLogs,blocks:renderBlocks,reset:renderReset};if(tab==="ai"){await renderAI();return}if(!u[tab])throw Error("Невідома вкладка");await f[tab](await api(u[tab]));}catch(e){msg(e.message,true)}}
function fmtTime(sec){sec=Math.max(0,Math.floor(Number(sec)||0));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60),s=sec%60;return `${String(h).padStart(2,"0")}:${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`}
function updateTimers(){document.querySelectorAll("[data-timer-id]").forEach(el=>{let elapsed=Number(el.dataset.elapsed||0);if(el.dataset.running==="1"&&el.dataset.started)elapsed+=Math.max(0,Math.floor((Date.now()-Date.parse(el.dataset.started))/1000));const total=Number(el.dataset.duration||60)*60,rem=Math.max(0,total-elapsed),e=el.querySelector("[data-elapsed]"),r=el.querySelector("[data-remaining]"),bar=el.querySelector("[data-progress]"),label=el.querySelector("[data-state]");if(e)e.textContent=fmtTime(Math.min(total,elapsed));if(r)r.textContent=fmtTime(rem);if(bar)bar.style.width=`${Math.min(100,total?elapsed/total*100:0)}%`;if(label)label.textContent=rem===0?"ЧАС ВИЙШОВ · ЗАВЕРШЕНО":el.dataset.running==="1"?"В РОБОТІ":el.dataset.finished==="1"?"ЗАВЕРШЕНО":el.dataset.paused==="1"?"ПРИЗУПИНЕНО":"НЕ РОЗПОЧАТО";if(rem===0&&el.dataset.running==="1"&&el.dataset.autofinished!=="1"){el.dataset.autofinished="1";workAct(Number(el.dataset.timerId),"finish_work")}})}
function timerBox(b){const finished=b.work_status==="finished"||b.status==="completed";return `<div class="admin-card" data-timer-id="${b.id}" data-elapsed="${b.elapsed_seconds||0}" data-started="${esc(b.work_started_at||"")}" data-running="${b.work_status==="running"?1:0}" data-paused="${b.work_status==="paused"?1:0}" data-finished="${finished?1:0}" data-duration="${b.duration||60}"><h4>⏱ Відлік часу роботи</h4><div style="display:flex;gap:18px;flex-wrap:wrap;font-size:18px"><span>Відпрацьовано: <b data-elapsed>${fmtTime(b.elapsed_seconds)}</b></span><span>Залишилось: <b data-remaining>${fmtTime(b.remaining_seconds)}</b></span><span data-state>${finished?"🏁 РОБОТУ ЗАВЕРШЕНО":b.work_status==="running"?"В РОБОТІ":b.work_status==="paused"?"ПРИЗУПИНЕНО":"НЕ РОЗПОЧАТО"}</span></div><div style="height:8px;background:#252a31;border-radius:99px;overflow:hidden;margin:10px 0"><div data-progress style="height:100%;width:${Math.min(100,((b.elapsed_seconds||0)/(Number(b.duration||60)*60))*100)}%;background:#ffbd00"></div></div><div class="admin-actions">${!finished&&b.work_status!=="running"?`<button class="btn primary" onclick="workAct(${b.id},'start_work')">▶️ Почати</button>`:""}${!finished&&b.work_status==="running"?`<button class="btn secondary" onclick="workAct(${b.id},'pause_work')">⏸ Пауза</button>`:""}${!finished?`<button class="btn primary" onclick="workAct(${b.id},'finish_work')">✔ Завершити роботу</button>`:""}${!finished?`<button class="btn secondary" onclick="workAct(${b.id},'reset_work')">↺ Скинути</button>`:""}</div></div>`}
async function workAct(id,action){try{const d=await postBooking(id,action);msg(d.message);load()}catch(e){msg(e.message,true)}}
async function renderBookings(a){let mechanics=[];try{mechanics=await api("/api/admin/mechanics")}catch(e){msg(e.message,true);return}$("#content").innerHTML=`<div class="admin-list">${a.length?a.map(b=>{const opts=mechanics.filter(m=>m.active&&(!m.service_ids.length||m.service_ids.includes(Number(b.service_id)))).map(m=>`<option value="${m.id}" ${Number(b.mechanic_id)===Number(m.id)?"selected":""}>${m.role==="chief"?"⭐":"🔧"} ${esc(m.name)}</option>`).join("");return `<article class="admin-card"><h3>#${b.id} · ${esc(b.name)}</h3><p>📞 ${esc(b.phone)}<br>🚗 ${esc(b.car)}<br>🔧 ${esc(b.service)} · <b>${b.duration} хв</b><br>📅 ${esc(b.date)} · ${esc(b.time)}<br>👥 Тип клієнта: <b>${b.client_type==="uklon"?"Uklon":"Звичайний клієнт"}</b></p><p>${esc(b.note||"")}</p><p><b>Механік:</b> ${b.mechanic_name?esc(b.mechanic_name):"не призначений"}</p><div class="admin-actions"><label>Тип клієнта <select class="client-type-select" data-id="${b.id}"><option value="regular" ${b.client_type!=="uklon"?"selected":""}>Звичайний клієнт</option><option value="uklon" ${b.client_type==="uklon"?"selected":""}>🚕 Uklon</option></select></label></div><form class="schedule-form" data-id="${b.id}" style="display:flex;gap:8px;flex-wrap:wrap;align-items:center"><select name="mechanic_id" required><option value="">Оберіть механіка</option>${opts}</select><input type="time" name="time" value="${/^\d{2}:\d{2}$/.test(b.time||"")?esc(b.time):""}" required><button class="btn primary">📅 Призначити час + механіка</button></form><p><b>Статус заявки:</b> ${esc(b.status)}</p><div class="admin-actions">${b.status!=="cancelled"&&b.status!=="completed"?`<button class="btn secondary" onclick="act(${b.id},'cancel')">Скасувати</button>`:""}${b.status==="cancelled"?`<button class="btn secondary" onclick="act(${b.id},'reopen')">Повернути</button>`:""}<a class="btn primary" href="tel:${esc(b.phone)}">📞 Дзвінок</a><button class="btn danger" onclick="act(${b.id},'archive')">🗑 В архів</button></div>${b.mechanic_id?timerBox(b):"<div class='admin-card'><b>Спочатку призначте механіка та час.</b></div>"}</article>`}).join(""):"<div class='admin-card'>Заявок немає.</div>"}</div>`;document.querySelectorAll(".schedule-form").forEach(f=>f.onsubmit=async e=>{e.preventDefault();const mechanic_id=Number(f.querySelector('[name=mechanic_id]').value),time=f.querySelector('[name=time]').value;try{const d=await postBooking(Number(f.dataset.id),"set_time",{mechanic_id,time});msg(d.message);load()}catch(x){msg(x.message,true)}});document.querySelectorAll(".client-type-select").forEach(s=>s.onchange=async()=>{try{const d=await postBooking(Number(s.dataset.id),"set_client_type",{client_type:s.value});msg(d.message);load()}catch(e){msg(e.message,true)}});updateTimers();timerInterval=setInterval(updateTimers,1000)}
async function act(id,action){if(action==="archive"&&!confirm("Перемістити в архів? Telegram-повідомлення також буде видалено."))return;try{const d=await postBooking(id,action);msg(d.message);load()}catch(e){msg(e.message,true)}}
function fmtDateTime(v){try{return new Date(v).toLocaleString("uk-UA",{dateStyle:"short",timeStyle:"short"})}catch{return String(v||"")}}
function renderCompletedWorks(a){const grouped={};for(const w of a||[]){const key=w.mechanic_name||"Без механіка";(grouped[key]??=[]).push(w)}const groups=Object.entries(grouped);$("#content").innerHTML=`<div class="admin-card"><h2>🏁 Виконані роботи</h2><p class="muted">Це окремий постійний журнал. Архівація заявки не видаляє виконану роботу, її час, механіка чи дані для статистики.</p><div class="admin-actions"><b>Усього виконаних: ${(a||[]).length}</b></div></div><div class="admin-list">${groups.length?groups.map(([mechanic,rows])=>`<section><div class="admin-card"><h3>👨‍🔧 ${esc(mechanic)} · ${rows.length} робіт</h3></div>${rows.map(w=>`<article class="admin-card"><h3>#${w.booking_id} · ${esc(w.service)}</h3><p>🚗 ${esc(w.car)} · 👤 ${esc(w.name)}<br>📅 ${esc(w.preferred_date)} · ${esc(w.preferred_time)}<br>⏱ Фактично: <b>${fmtTime(w.elapsed_seconds)}</b> · Норма: ${w.duration_minutes} хв<br>💰 ${w.final_price==null?"Ціна не встановлена":`${Number(w.final_price).toFixed(0)} грн`}<br>🏁 Завершено: ${esc(fmtDateTime(w.completed_at))}</p>${w.comment?`<p>📝 ${esc(w.comment)}</p>`:""}</article>`).join("")}</section>`).join(""):"<div class='admin-card'>Виконаних робіт ще немає.</div>"}</div>`}
function renderHistory(a){$("#content").innerHTML=`<div class="admin-actions"><button class="btn danger" ${role!=="superadmin"?"disabled":""} onclick="clearHistory()">🗑 Очистити архів</button></div><div class="admin-list">${a.length?a.map(x=>`<article class="admin-card"><h3>#${x.id} · ${esc(x.name)}</h3><p>🚗 ${esc(x.car)} · 🔧 ${esc(x.service)}<br>📅 ${esc(x.date)} ${esc(x.time)}<br>Архівовано: ${esc(x.archived_at)}</p></article>`).join(""):"<div class='admin-card'>Архів порожній.</div>"}</div>`}
async function clearHistory(){if(role!=="superadmin"||!confirm("Очистити архів?"))return;try{const d=await api("/api/admin/history",{method:"DELETE"});msg(d.message);load()}catch(e){msg(e.message,true)}}
function renderReviews(a){$("#content").innerHTML=`<div class="admin-list">${a.length?a.map(r=>`<article class="admin-card"><h3>${esc(r.name)} · ${"★".repeat(r.rating)}${"☆".repeat(5-r.rating)}</h3><p>${esc(r.text)}</p><b>${r.published?"Опубліковано":"На модерації"}</b><div class="admin-actions">${r.published?`<button class="btn secondary" onclick="review(${r.id},'hide')">Приховати</button>`:`<button class="btn primary" onclick="review(${r.id},'approve')">Опублікувати</button>`}<button class="btn danger" onclick="review(${r.id},'delete')">🗑 Видалити</button></div></article>`).join(""):"<div class='admin-card'>Відгуків немає.</div>"}</div>`}
async function review(id,action){if(action==="delete"&&!confirm("Видалити відгук?"))return;try{const d=await api("/api/admin/reviews",{method:"POST",body:JSON.stringify({id,action})});msg(d.message);load()}catch(e){msg(e.message,true)}}
async function renderWorks(a){
  let drive={connected:false};
  try{drive=await api("/api/media/status")}catch(e){}
  const driveStatus=drive.connected&&drive.folder_ok?`<span class="muted">✅ Google Drive підключено · папка: ${esc(drive.folder_name||drive.folder_id||"")}</span>`:drive.connected?`<span class="muted">⚠️ OAuth підключений, але папка недоступна: ${esc(drive.folder_error||"перевірте доступ")}</span>`:`<span class="muted">⚠️ Google Drive не підключено</span>`;
  $("#content").innerHTML=`<div class="admin-card"><h2>☁️ Сховище медіа</h2><p>${driveStatus}</p><div class="admin-actions">${drive.connected?"":`<button class="btn primary" id="driveConnect">☁️ Підключити Google Drive</button>`}</div><p class="muted">Файли зберігаються у твоїй папці Google Drive. На сайті показується тільки фото/відео без інтерфейсу Google або YouTube.</p></div><form id="workForm" class="form admin-form" enctype="multipart/form-data"><h2>🔧 Додати виконану роботу</h2><input name="title" placeholder="Назва роботи" required><input name="car" placeholder="Автомобіль"><select name="media_type" id="workMediaType"><option value="image">📷 Фото</option><option value="video">🎥 Відео</option></select><div id="workPlayerBox" class="video-source-controls hidden"><label><span>🎬 Плеєр для відео</span><select name="player_type" id="workPlayerType"><option value="html5">📁 HTML5 — файл / Google Drive</option><option value="youtube">▶️ YouTube</option><option value="youtube_nocookie">🔒 YouTube No-Cookie</option><option value="instagram">📷 Instagram Reel</option></select></label><input name="external_video_url" id="externalVideoUrl" type="url" class="hidden" placeholder="https://youtu.be/... або https://www.instagram.com/reel/..."><small class="media-player-help">YouTube та Instagram використовують URL, файл завантажувати не потрібно.</small></div><input name="file" id="workFile" type="file" accept="image/jpeg,image/png,image/webp,image/gif,video/mp4,video/webm,video/quicktime" required><input name="instagram_url" placeholder="Instagram цієї роботи (необов'язково)"><textarea name="description" placeholder="Опис роботи"></textarea><button class="btn primary">☁️ Завантажити і додати роботу</button><div id="workUploadMsg" class="muted"></div></form><div class="admin-list">${a.length?a.map(w=>`<article class="admin-card work-admin"><div class="work-admin-media">${w.media_type==="video"?`<video class="work-admin-img" src="${esc(w.media_url)}" controls preload="metadata" playsinline></video>`:`<img class="work-admin-img" src="${esc(w.media_url||w.image_url)}" alt="${esc(w.title)}">`}</div><div><h3>${esc(w.title)}</h3><p>🚗 ${esc(w.car||"")}<br>${esc(w.description||"")}</p>${w.instagram_url?`<a class="btn secondary" href="${esc(w.instagram_url)}" target="_blank" rel="noopener noreferrer">📷 Instagram</a>`:""} <button class="btn danger" onclick="workDelete(${w.id})">🗑 Видалити</button></div></article>`).join(""):"<div class='admin-card'>Робіт немає.</div>"}</div>`;
  const connect=$("#driveConnect");if(connect)connect.onclick=()=>{location.href="/api/google/start"};
  const mediaTypeEl=$("#workMediaType"),playerEl=$("#workPlayerType"),playerBox=$("#workPlayerBox"),fileEl=$("#workFile"),externalEl=$("#externalVideoUrl");
  function syncWorkMedia(){const video=mediaTypeEl.value==="video";playerBox.classList.toggle("hidden",!video);const external=video&&playerEl.value!=="html5";externalEl.classList.toggle("hidden",!external);externalEl.required=external;fileEl.required=!external;fileEl.disabled=external;}
  mediaTypeEl.onchange=syncWorkMedia;playerEl.onchange=syncWorkMedia;syncWorkMedia();
  $("#workForm").onsubmit=async e=>{e.preventDefault();const form=e.target,msgEl=$("#workUploadMsg"),kind=form.querySelector('[name=media_type]').value,player=form.querySelector('[name=player_type]').value;try{let mediaUrl="";if(kind==="video"&&player!=="html5"){mediaUrl=form.querySelector('[name=external_video_url]').value.trim();if(!mediaUrl)throw Error("Вкажіть URL відео для вибраного плеєра");}else{const file=form.querySelector('[name=file]').files[0];// R2 is the primary media store. Google Drive is an additional archive and may be temporarily unavailable.if(!file)throw Error("Виберіть файл");const expected=kind==="video"?file.type.startsWith("video/"):file.type.startsWith("image/");if(!expected)throw Error("Тип файлу не відповідає вибраному типу медіа");msgEl.textContent="⏳ Завантаження у Google Drive + R2…";const fd=new FormData();fd.append("file",file,file.name);fd.append("media_type",kind);const up=await api("/api/media/upload",{method:"POST",body:fd});mediaUrl=up.url;
if(up.storage?.google_drive){
  msgEl.textContent="✅ Файл збережено у Google Drive + R2. ⏳ Зберігаємо роботу…";
}else{
  msgEl.textContent="✅ Файл збережено у R2. ⚠️ Google Drive не записав файл. ⏳ Зберігаємо роботу…";
}
}msgEl.textContent=msgEl.textContent||"⏳ Зберігаємо роботу…";const b=Object.fromEntries(new FormData(form));const rr=await api("/api/admin/works",{method:"POST",body:JSON.stringify({title:b.title,car:b.car,description:b.description,instagram_url:b.instagram_url,media_type:kind,media_url:mediaUrl,player_type:kind==="video"?player:"html5"})});msg(rr.message);load()}catch(x){msgEl.textContent="";msg(x.message,true)}};
}
async function workDelete(id){if(!confirm("Видалити роботу?"))return;try{const d=await api("/api/admin/works?id="+id,{method:"DELETE"});msg(d.message);load()}catch(e){msg(e.message,true)}}
function renderServices(a){const rows=a.filter(x=>x.name!=="Uklon");$("#content").innerHTML=`<div class="admin-card"><h2>💰 Послуги та ціни</h2><p class="muted">Uklon прихований від клієнта. Тут задається тривалість роботи, яка використовується таймером механіка.</p></div><div class="admin-list">${rows.map(s=>`<article class="admin-card"><form class="service-form" data-id="${s.id}"><h3>🔧 ${esc(s.name)}</h3><input name="name" value="${esc(s.name)}" required><input name="description" value="${esc(s.description||"")}" placeholder="Опис"><input name="price_from" type="number" min="0" step="1" value="${s.price_from??""}" placeholder="Ціна від, грн" ${role!=="superadmin"?"disabled":""}><label>⏱ Норма роботи, хв<input name="duration_minutes" type="number" min="15" max="1440" step="15" value="${s.duration_minutes}" ${role!=="superadmin"?"disabled":""}></label><label><input name="active" type="checkbox" ${s.active?"checked":""} ${role!=="superadmin"?"disabled":""}> Активна</label>${role==="superadmin"?`<button class="btn primary">💾 Зберегти</button>`:`<p class="muted">Перегляд для admin</p>`}</form></article>`).join("")}</div>`;document.querySelectorAll(".service-form").forEach(f=>f.onsubmit=async e=>{e.preventDefault();if(role!=="superadmin")return;const d=Object.fromEntries(new FormData(f));d.id=Number(f.dataset.id);d.active=f.querySelector('[name=active]').checked;try{const r=await api("/api/admin/services",{method:"POST",body:JSON.stringify(d)});msg(r.message);load()}catch(x){msg(x.message,true)}})}
async function renderMechanics(a){let services=[];try{services=await api("/api/admin/services")}catch(e){msg(e.message,true);return}const rows=services.filter(s=>s.name!=="Uklon"&&s.active);$("#content").innerHTML=`<div class="admin-card"><h2>👨‍🔧 Механіки СТО</h2><p>Закріпіть за кожним механіком ті види робіт, які він виконує. Після цього в заявці можна буде обрати механіка лише для дозволеної послуги.</p><form id="mechanicForm" class="form admin-form"><input name="name" placeholder="Ім'я механіка" required><select name="role"><option value="mechanic">Звичайний механік</option><option value="chief">Головний механік</option></select><button class="btn primary">Додати</button></form></div><div class="admin-list">${a.map(m=>`<article class="admin-card"><form class="mechanic-form" data-id="${m.id}"><h3>${m.role==="chief"?"⭐":"🔧"} ${esc(m.name)}</h3><input name="name" value="${esc(m.name)}" required><select name="role"><option value="mechanic" ${m.role!=="chief"?"selected":""}>Звичайний механік</option><option value="chief" ${m.role==="chief"?"selected":""}>Головний механік</option></select><label><input name="active" type="checkbox" ${m.active?"checked":""}> Активний</label><h4>🛠 Види робіт:</h4><div style="display:grid;gap:6px">${rows.map(s=>`<label><input type="checkbox" name="service_ids" value="${s.id}" ${m.service_ids.includes(Number(s.id))?"checked":""}> ${esc(s.name)} <small>(${s.duration_minutes} хв)</small></label>`).join("")}</div><button class="btn primary">💾 Зберегти механіка</button></form></article>`).join("")}</div>`;$("#mechanicForm").onsubmit=saveMechanic;document.querySelectorAll(".mechanic-form[data-id]").forEach(f=>f.onsubmit=saveMechanic)}
async function saveMechanic(e){e.preventDefault();const f=e.target,d={name:f.querySelector('[name=name]').value.trim(),role:f.querySelector('[name=role]').value,active:f.querySelector('[name=active]').checked,service_ids:[...f.querySelectorAll('[name=service_ids]:checked')].map(x=>Number(x.value))};if(f.dataset.id)d.id=Number(f.dataset.id);try{const r=await api("/api/admin/mechanics",{method:"POST",body:JSON.stringify(d)});msg(r.message);load()}catch(x){msg(x.message,true)}}
async function renderAI(){
  const opts=[
    ["Підготувати відповідь клієнту","Допоможи ввічливо відповісти клієнту на основі контексту заявки. Не обіцяй результатів, яких ми не підтвердили."],
    ["Підсумувати заявку","Стисло структуруй заявку: що відомо, що треба уточнити, що перевірити механіку."],
    ["Опис виконаної роботи","Створи короткий професійний опис виконаної роботи для розділу «Наші роботи». Не вигадуй деталей."],
    ["SEO-текст","Створи короткий SEO-текст для послуги СТО в Полтаві без вигаданих цифр, гарантій чи фактів."]
  ];
  $("#content").innerHTML=`<div class="admin-card"><h2>🤖 AI-помічник</h2><p class="muted">AI допомагає адміністратору з текстами та структурою заявки. Діагностику і рішення щодо ремонту завжди приймає механік.</p><div class="admin-actions">${opts.map((x,i)=>`<button class="btn secondary ai-preset" data-i="${i}">${esc(x[0])}</button>`).join("")}</div><div style="display:grid;gap:10px;margin-top:14px"><label>№ заявки, якщо потрібно <input id="aiBooking" type="number" min="1" placeholder="Наприклад 31"></label><label>Завдання для AI <textarea id="aiTask" rows="7" placeholder="Напишіть, що потрібно зробити..."></textarea></label><button id="aiRun" class="btn primary">🤖 Запустити AI</button><div id="aiResult" class="admin-card" style="white-space:pre-wrap;display:none"></div></div></div>`;
  document.querySelectorAll('.ai-preset').forEach(btn=>btn.onclick=()=>{$('#aiTask').value=opts[Number(btn.dataset.i)][1];$('#aiTask').focus()});
  $('#aiRun').onclick=async()=>{const task=$('#aiTask').value.trim(),booking_id=Number($('#aiBooking').value||0),out=$('#aiResult');if(!task){msg('Вкажіть завдання для AI',true);return}out.style.display='block';out.textContent='⏳ AI аналізує запит…';try{const d=await api('/api/admin/ai',{method:'POST',body:JSON.stringify({task,booking_id:booking_id||undefined})});out.textContent=d.answer||'AI не повернув відповідь';}catch(e){out.textContent='❌ '+e.message;}}
}

async function renderAnalytics(){
 const d=await api("/api/admin/analytics"); const rows=d.mechanics||[];
 $("#content").innerHTML=`<div class="admin-card"><h2>📊 Аналітика СТО</h2><p>Фактичні роботи, нормативний час, завантаження та виручка за завершеними роботами.</p>
 <div class="admin-actions"><b>Усього заявок: ${d.total?.jobs||0}</b><b>Виконано: ${d.total?.completed||0}</b><b>Фактичний час: ${(Number(d.total?.elapsed_seconds||0)/3600).toFixed(2)} год</b></div></div>
 <div class="admin-list">${rows.map(x=>`<article class="admin-card"><h3>${x.role==="chief"?"⭐":"🔧"} ${esc(x.name)}</h3><p>Виконано: <b>${x.completed}</b> · Заявок: ${x.jobs} · Скасовано: ${x.cancelled}</p><p>Норма: ${x.norm_hours} год · Фактично: ${x.elapsed_hours} год · Виконання: ${x.utilization}%</p><p>Виручка робіт: <b>${Number(x.revenue||0).toFixed(0)} грн</b></p></article>`).join("")||"Немає даних."}</div><div class="admin-actions"><button class="btn primary" onclick="tab='completed';document.querySelectorAll('.admin-tabs button').forEach(x=>x.classList.toggle('active',x.dataset.tab==='completed'));load()">🏁 Відкрити всі виконані роботи</button></div>`;
}
async function renderMarket(){
 const d=await api("/api/admin/market");
 $("#content").innerHTML=`<div class="admin-card"><h2>💵 Ринок та курс USD</h2><p>USD оновлюється щогодини з НБУ. Ринкові джерела аналізуються щодня. AI лише рекомендує ціну, а застосовує її тільки superadmin.</p>
 <p>Останній USD: <b>${d.usd?Number(d.usd.rate).toFixed(4):"ще немає даних"} грн</b> ${d.usd?`· ${esc(d.usd.fetched_at)}`:""}</p>
 ${role==="superadmin"?`<button class="btn primary" id="marketSync">🔄 Оновити зараз</button><form id="marketSource" class="form admin-form" style="margin-top:12px"><input name="name" placeholder="Назва джерела" required><input name="url" type="url" placeholder="https://..." required><button class="btn secondary">Додати джерело</button></form>`:"<p class='muted'>Зміни цін та джерел доступні лише superadmin.</p>"}</div>
 <div class="admin-card"><h3>💡 Рекомендації AI</h3>${(d.recommendations||[]).map(r=>`<article class="admin-card"><b>${esc(r.service_name)}</b><p>${r.old_price==null?"Ціна не встановлена":`${r.old_price} грн`} → <strong>${r.new_price} грн</strong></p><p>Ринок: ${r.market_min??"?"}–${r.market_max??"?"} грн, медіана ${r.market_median??"?"} грн</p><p>${esc(r.reason||"")}</p>${role==="superadmin"?`<button class="btn primary" onclick="applyPrice(${r.id})">Застосувати</button>`:""}</article>`).join("")||"<p>Поки немає рекомендацій.</p>"}</div>
 <div class="admin-list">${(d.sources||[]).map(s=>`<article class="admin-card"><b>${esc(s.name)}</b><br><small>${esc(s.url)}</small></article>`).join("")}</div>
 <div class="admin-list">${(d.snapshots||[]).slice(0,10).map(s=>`<article class="admin-card"><h3>${esc(s.source_name)}</h3><small>${esc(s.fetched_at)} · ${esc(s.status)}</small><p>${esc(s.ai_summary||"Без AI-висновку")}</p></article>`).join("")||"<div class='admin-card'>Ще немає ринкових знімків.</div>"}</div>`;
 if(role==="superadmin"){
  $("#marketSync").onclick=async()=>{try{const r=await api("/api/admin/market",{method:"POST",body:JSON.stringify({action:"sync"})});msg(`USD ${Number(r.rate).toFixed(4)} · джерел: ${r.updated_sources} · рекомендацій: ${r.recommendations.length}`);load()}catch(e){msg(e.message,true)}};
  $("#marketSource").onsubmit=async e=>{e.preventDefault();try{const b=Object.fromEntries(new FormData(e.target));const r=await api("/api/admin/market",{method:"POST",body:JSON.stringify({action:"add_source",...b})});msg(r.message);load()}catch(x){msg(x.message,true)}};
 }
}
async function applyPrice(id){if(role!=="superadmin"||!confirm("Застосувати рекомендовану ціну?"))return;try{const r=await api("/api/admin/market",{method:"POST",body:JSON.stringify({action:"apply_price",id})});msg(r.message);load()}catch(e){msg(e.message,true)}}

async function renderReset(){
 if(role!=="superadmin"){
  $("#content").innerHTML=`<div class="admin-card"><h2>🔒 Скидання даних</h2><p>Доступ дозволений лише superadmin.</p></div>`;
  return;
 }
 const scopes=[
  ["bookings","📅 Заявки","Нові заявки, призначення та пов'язані Telegram-повідомлення"],
  ["archive","🗂 Архів","Архівовані заявки"],
  ["completed","🏁 Виконані роботи","Виконані роботи, час, статистика та Telegram-записи"],
  ["reviews","⭐ Відгуки","Відгуки клієнтів"],
  ["blocks","⛔ Блокування","Заблоковані часові слоти"],
  ["logs","📋 Журнал","Журнал адміністратора, крім самого запису про reset"],
  ["media","☁️ Медіа","Медіа-об'єкти та їхні записи"],
  ["works","🔧 Роботи","Опубліковані виконані роботи та прив'язані медіа"],
  ["all_operational","🧹 УСІ операційні дані","Усе вище, але НЕ послуги, ціни, механіки, OAuth або налаштування"]
 ];
 $("#content").innerHTML=`
 <div class="admin-card">
  <h2>🧹 Центр безпечного скидання</h2>
  <p class="muted">Тут можна підготувати сайт до передачі. Скидання вибіркове. Каталог послуг, ціни, механіки, Google OAuth та системні налаштування захищені.</p>
  <div class="admin-list">${scopes.map(([id,title,desc])=>`<article class="admin-card" style="margin:0 0 10px">
   <h3>${title}</h3><p class="muted">${desc}</p>
   <div class="admin-actions"><button class="btn secondary" onclick="resetPreview('${id}')">🔎 Перевірити</button><button class="btn danger" onclick="resetExecute('${id}')">🗑 Стерти</button></div>
   <div id="reset-preview-${id}" style="margin-top:10px"></div>
  </article>`).join("")}</div>
 </div>`;
}
async function resetPreview(scope){
 try{
  const d=await api(`/api/admin/reset?scope=${encodeURIComponent(scope)}`);
  const el=document.querySelector(`#reset-preview-${scope}`);
  if(el)el.innerHTML=`<div class="admin-card"><b>Буде видалено:</b><pre style="white-space:pre-wrap">${esc(JSON.stringify(d.counts,null,2))}</pre><span class="muted">Захищено: ${esc((d.protected||[]).join(", "))}</span></div>`;
 }catch(e){msg(e.message,true)}
}
async function resetExecute(scope){
 if(role!=="superadmin")return msg("Потрібні права superadmin",true);
 try{
  const preview=await api(`/api/admin/reset?scope=${encodeURIComponent(scope)}`);
  const counts=JSON.stringify(preview.counts,null,2);
  const ok=confirm(`Скидання: ${scope}\n\nПеред видаленням буде очищено:\n${counts}\n\nПродовжити?`);
  if(!ok)return;
  const password=prompt("Для продовження введіть пароль SUPERADMIN:");
  if(password===null)return;
  const confirmText=prompt(`Останній захист. Введіть точно:\nRESET:${scope}`);
  if(confirmText===null)return;
  const d=await api("/api/admin/reset",{method:"POST",body:JSON.stringify({action:"execute",scope,password,confirm:confirmText})});
  msg(`${d.message}. Дані до: ${JSON.stringify(d.before)}; після: ${JSON.stringify(d.after)}`);
  await load();
 }catch(e){msg(e.message,true)}
}

function renderLogs(a){$("#content").innerHTML=`<div class="admin-card">${a.map(x=>`<div class="log-row">${esc(x.created_at)} · <b>${esc(x.actor)}</b> · ${esc(x.action)} · ${esc(x.target)} · ${esc(x.details)}</div>`).join("")||"Журнал порожній."}</div>`}
function renderBlocks(a){$("#content").innerHTML=`<form id="blockForm" class="form admin-form"><h2>Заблокувати час</h2><input type="date" name="date" required><input type="time" name="time" required><input name="reason" placeholder="Причина"><button class="btn primary">Заблокувати</button></form><div class="admin-list">${a.map(x=>`<article class="admin-card">${esc(x.date)} · ${esc(x.time)} · ${esc(x.reason||"")} <button class="btn danger" onclick="blockDelete(${x.id})">Видалити</button></article>`).join("")}</div>`;$("#blockForm").onsubmit=async e=>{e.preventDefault();try{const d=Object.fromEntries(new FormData(e.target)),r=await api("/api/admin/blocks",{method:"POST",body:JSON.stringify(d)});msg(r.message);load()}catch(x){msg(x.message,true)}}}
async function blockDelete(id){try{const d=await api("/api/admin/blocks?id="+id,{method:"DELETE"});msg(d.message);load()}catch(e){msg(e.message,true)}}
document.querySelectorAll(".admin-tabs button").forEach(b=>b.onclick=()=>{tab=b.dataset.tab;document.querySelectorAll(".admin-tabs button").forEach(x=>x.classList.toggle("active",x===b));load()});$("#loginBtn").onclick=login;$("#loginPass").onkeydown=e=>{if(e.key==="Enter")login()};$("#refresh").onclick=load;$("#logout").onclick=async()=>{await fetch("/api/auth/logout",{method:"POST",credentials:"same-origin"});location.reload()};
