const JSON_HEADERS={'content-type':'application/json; charset=utf-8','access-control-allow-origin':'*','access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'content-type'};
function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:JSON_HEADERS})}
export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/reviews'){if(request.method==='OPTIONS')return new Response(null,{headers:JSON_HEADERS});if(!env.DB)return json({error:'D1 database is not configured'},503);if(request.method==='GET'){const {results}=await env.DB.prepare('SELECT id,name,rating,text,created_at FROM reviews WHERE approved=1 ORDER BY created_at DESC LIMIT 100').all();return json(results)}if(request.method==='POST'){try{const body=await request.json();const name=String(body.name||'').trim()||'Анонім';const text=String(body.text||'').trim();const rating=Number(body.rating);if(name.length>60||!text||text.length>1000||rating<1||rating>5)return json({error:'Некоректні дані'},400);await env.DB.prepare('INSERT INTO reviews (name,rating,text,approved) VALUES (?1,?2,?3,0)').bind(name,rating,text).run();return json({ok:true},201)}catch{return json({error:'Помилка сервера'},500)}}return json({error:'Method not allowed'},405)}return env.ASSETS.fetch(request)}};
